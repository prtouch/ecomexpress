require 'spec_helper'

describe Ecomexpress do
  it 'has a version number' do
    expect(Ecomexpress::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
